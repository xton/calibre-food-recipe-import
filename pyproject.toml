[project]
name = "calibre-import-recipe"
version = "0.1.0"
description = "Calibre plugin: import food recipes from URLs as clean EPUBs"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest>=7"]

[tool.pytest.ini_options]
testpaths = ["tests"]
